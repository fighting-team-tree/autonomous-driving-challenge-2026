@AGENTS.md

## Claude Code 전용

- 문서 작업 시 `docs/` 아래 파일을 새로 만들면 `mkdocs.yml`의 `nav`도 함께 갱신한다. CI가 `--strict`라 누락되면 배포가 실패한다.
- 실험 기록을 대신 작성해달라는 요청을 받으면, 결과부터 채우지 말고 `배경 / 가설 / 실험 설계`까지만 작성하고 멈춘다. 결과는 학습을 돌린 사람이 채운다.
